'use strict';

describe('gemini', () => {
    it('should do nothing if plugin is disabled', () => {

    });

    it('should create a config decorator', () => {

    });

    it('should create a retry limiter', () => {

    });

    it('should disable retries if retries count become over a limit', () => {

    });

    it('should not disable retries of retries count does not become over a limit', () => {

    });
});
