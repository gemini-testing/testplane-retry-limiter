'use strict';

describe('lib/config-decorator', () => {
    describe('.create()', () => {
        it('should return an instance of a config decorator', () => {

        });
    });

    describe('.prototype.disableRetries()', () => {
        it('should disable retries in all browsers', () => {

        });
    });
});
