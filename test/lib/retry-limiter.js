'use strict';

describe('lib/retry-limiter', () => {
    describe('.create()', () => {
        it('should create an instance of a retry limiter', () => {

        });
    });

    describe('.totalTestsCount', () => {
        it('should be zero by default', () => {

        });

        it('should be public to modify', () => {

        });
    });

    describe('.prototype.isOverLimit()', () => {
        it('should return "false" in case of under limit', () => {

        });

        it('should return "true" in case of over limit', () => {

        });
    });
});
